# BGN⇄EUR Scan (full‑stack)

Телефонно приложение (Expo) + backend (Node/Express), което:
- конвертира **BGN ⇄ EUR** по **фиксиран курс 1 EUR = 1.95583 BGN**
- снима цена от етикет/бележка и **извлича сумата (OCR)**
- позволява да **stack-ваш** (натрупваш) много суми и показва **общо** в двете валути

## 1) Стартирай backend (OCR)
```bash
cd backend
npm install
npm run dev
```
Backend по подразбиране тръгва на `http://localhost:4000`

## 2) Стартирай mobile app
```bash
cd mobile
npm install
npm run start
```
Отвори Expo Go на телефона и сканирай QR кода от терминала.

## 3) Настройки (много важно за телефон)
В приложението → **Настройки** → задай Backend URL:

- Ако тестваш на емулатор: `http://10.0.2.2:4000` (Android emulator)
- Ако си на истински телефон: `http://192.168.X.X:4000` (IP на компютъра в същата Wi‑Fi мрежа)

## Бележки
- OCR никога не е 100% (особено при отражения/лош фокус). Затова има:
  - кандидати за цена (тапни за избор)
  - ръчно въвеждане

Успех! 🙂
